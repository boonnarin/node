<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://unpkg.com/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/stys.css">
    <link rel="stylesheet" href="css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns"></script>

</head>

<body>
    <%- include('headerlog') %>
    <button id="openBar" class="open-btn">☰ </button>
    <div id="mySidebar" class="sidebar">
        <a href="#">Link 1</a>
        <a href="#">Link 2</a>
        <a href="#">Link 3</a>
        <a href="#">Link 4</a>
    </div>

    <div id="main">
        <div class="container mt-4">
            <h1><%= title %></h1>
            
            <div id="chart-container">
                <canvas id="myChart" style="max-width: 100%; height: 400px;"></canvas>
            </div>
            
        </div>
    </div>

    <%- include('footer') %>

    <!-- เพิ่มการแทรกข้อมูล measurements -->
    <script>
        var measurements = <%- JSON.stringify(measurements) %>;
    </script>
    <script src="js/chart.js"></script> <!-- นำเข้าไฟล์ main.js -->
    <script src="js/sidebar.js"></script> <!-- นำเข้าไฟล์ bar.js -->

    <script>
        document.addEventListener('DOMContentLoaded', (event) => {
            var loginButton = document.getElementById('loginbutton');
            var userName = localStorage.getItem('username'); // สมมุติว่าชื่อผู้ใช้ถูกจัดเก็บใน Local Storage

            if (userName) {
                loginButton.textContent = userName;
                loginButton.href = "/profile"; // เปลี่ยนลิงค์ไปยังหน้าโปรไฟล์ผู้ใช้
            }
        });

        // ตัวอย่างวิธีการตั้งค่า Local Storage เมื่อผู้ใช้ SignIn
        // นี่เป็นเพียงตัวอย่าง ในการใช้งานจริงคุณจะต้องทำการตั้งค่าเมื่อผู้ใช้ SignIn สำเร็จ
        function signIn(username) {
            localStorage.setItem('username', username);
            window.location.reload(); // รีโหลดหน้าเพื่ออัพเดตปุ่ม SignIn
        }

        // ตัวอย่างการเรียกฟังก์ชัน signIn (ควรเรียกใช้เมื่อผู้ใช้ SignIn สำเร็จ)
        // signIn('JohnDoe');

        
    </script>
</body>
</html>
